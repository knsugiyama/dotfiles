#!/usr/bin/env bash

set -eu

. ${HOME}/.dotfiles/etc/scripts/lib/os_detect.sh

OS=$(os_detect)

if [ ${OS} == 'osx' ]; then
  brew uninstall cf-cli@8

elif [ ${OS} == 'linux' ]; then
  sudo apt remove cf8-cli

fi
